# RustFS credentials — encrypt with: sops encrypt secrets/server/rustfs.yaml
# These credentials are also used by niks3 as S3 access/secret keys.
rustfs-access-key: rustfsadmin
rustfs-secret-key: rustfsadmin
